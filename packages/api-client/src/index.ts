// OpenAPI-derived types (generated)
export * from "./schema";

// TODO: add a typed fetch client wrapper around the daemon REST API.
// Suggestion: implement `createClient({ baseUrl, token })` that returns typed methods.
