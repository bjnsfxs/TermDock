{
  "name": "ai-cli-manager",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "web",
    "client",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "pnpm -C web dev",
    "dev:desktop": "pnpm -C client dev",
    "build:web": "pnpm -C web build",
    "build:desktop": "pnpm -C client build",
    "ci:daemon": "cargo test --manifest-path daemon/Cargo.toml",
    "ci:web": "pnpm -C web test && pnpm -C web build",
    "ci:desktop": "pnpm -C client build",
    "ci:portable": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/release/build-portable.ps1",
    "release:portable": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/release/build-portable.ps1",
    "dev:daemon": "cargo run --manifest-path daemon/Cargo.toml",
    "lint": "echo \"Add lint scripts\""
  }
}
